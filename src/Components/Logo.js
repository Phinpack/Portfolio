import React from 'react';
import { Button } from 'reactstrap';

const Logo = () => {
  return (
    <Button
      style={{
        color: 'white',
        fontSize: '55px',
        fontWeight: '500',
        backgroundColor: 'rgba(0, 0, 0, 0.0)', 
        border: 'none',
        marginLeft: '60px',
      }}
    >MetalityLabs
    </Button>
  )
};

export default Logo;
