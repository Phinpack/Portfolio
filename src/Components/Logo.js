import React from 'react';
import { Button } from 'reactstrap';

const Logo = () => {
  return (
    <Button
      style={{
        color: 'black',
        fontSize: '49px',
        backgroundColor: 'rgba(0, 0, 0, 0.0)', 
        border: 'none'
      }}
    >Intelimmerse
    </Button>
  )
};

export default Logo;
